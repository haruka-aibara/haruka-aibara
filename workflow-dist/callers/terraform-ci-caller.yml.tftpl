# Managed by Terraform (haruka-aibara/works). Do not edit directly.
name: Terraform CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    uses: ${reusable}
    with:
      working-directory: "${working_directory}"
