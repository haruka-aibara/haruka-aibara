# Managed by Terraform (haruka-aibara/works). Do not edit directly.
name: Terraform CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    uses: haruka-aibara/github-actions/.github/workflows/terraform-ci.yml@main
    with:
      working-directory: "${working_directory}"
